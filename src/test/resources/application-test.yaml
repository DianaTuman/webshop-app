spring:
  application:
    name: webshop-app-test

  servlet:
    multipart:
      max-request-size: 418018841
      max-file-size: 20848820
      file-size-threshold: 1048576

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect